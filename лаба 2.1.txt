#include <stdio.h>
#include <conio.h>
#include <windows.h>
struct toys 
{
	char NameOfToy[100];
	int price;
	int age1;
	int age2;
};
int main()
{
	int ctr;
	struct toys children[3];
	for(ctr=0;ctr<3;ctr++)
	{
		printf("Enter the name of the %d toy:\n",(ctr+1));
		gets_s(children[ctr].NameOfToy);
		puts("How much is it?");
		scanf_s("%d", &children[ctr].price);
		printf("Enter the category:");
		scanf_s("%d", &children[ctr].age1);
		scanf_s("%d", &children[ctr].age2);
	}
	system("cls");
	fflush(stdin);
	printf("\n My toys: \n");
	for (ctr = 0; ctr < 3; ctr++)
	{
		printf("№%d: %s-name of the toy\n", (ctr + 1), children[ctr].NameOfToy);
		printf("%d - price\n",children[ctr].price);
		printf("%d-%d - age category\n;",children[ctr].age1,children[ctr].age2);
	}
	return 0;
}


==============================================================================================
#include <stdio.h>
#include <conio.h>
#include <windows.h>
struct toys 
{
	int price;
	int age1;
	int age2;
};
int main()
{
	system("chcp 1251>0");
	int input;
	printf("Выбери игрушку:\n 1.Кукла\n 2.Кубик\n 3.Мяч\n 4.Конструктор\n");
	printf("Я выбираю:");
	scanf_s("%d", &input);
	system("cls");
	switch (input)
	{
	case 1:
	{
		struct toys children[4];
		int ctr;
		printf("Ваш выбор пал на куклу\n");
		for (ctr = 0; ctr < 3; ctr++)
		{
			printf("Введите цену игрушки %d:", (ctr + 1));
			scanf_s("%d", &children[ctr].price);
			puts("Введите возрастную категорию:");
			scanf_s("%d%d", &children[ctr].age1, &children[ctr].age2);
			system("cls");
		}
		printf("Информация о товаре Кукла:\n");
		for (ctr = 0; ctr < 3; ctr++)
		{
			printf("Товар №%d:\n", (ctr + 1));
			printf("Цена:%d Возрасная категория:%d-%d\n", children[ctr].price, children[ctr].age1, children[ctr].age2);
		}
		break;
	}
	case 2:
	{
		struct toys children[4];
		int ctr;
		printf("Ваш выбор пал на Кубик\n");
		for (ctr = 0; ctr < 3; ctr++)
		{
			printf("Введите цену игрушки %d:", (ctr + 1));
			scanf_s("%d", &children[ctr].price);
			puts("Введите возрастную категорию:");
			scanf_s("%d%d", &children[ctr].age1, &children[ctr].age2);
			system("cls");
		}
		printf("Информация о товаре Кубик:\n");
		for (ctr = 0; ctr < 3; ctr++)
		{
			printf("Товар №%d:\n", (ctr + 1));
			printf("Цена:%d Возрасная категория:%d-%d\n", children[ctr].price, children[ctr].age1, children[ctr].age2);
		}
		break;
	}
	case 3:
	{
		struct toys children[4];
		int ctr;
		printf("Ваш выбор пал на Мяч\n");
		for (ctr = 0; ctr < 3; ctr++)
		{
			printf("Введите цену игрушки %d:", (ctr + 1));
			scanf_s("%d", &children[ctr].price);
			puts("Введите возрастную категорию:");
			scanf_s("%d%d", &children[ctr].age1, &children[ctr].age2);
			system("cls");
		}
		printf("Информация о товаре Мяч:\n");
		for (ctr = 0; ctr < 3; ctr++)
		{
			printf("Товар №%d:\n", (ctr + 1));
			printf("Цена:%d Возрасная категория:%d-%d\n", children[ctr].price, children[ctr].age1, children[ctr].age2);
		}
		break;
	}
	case 4:
	{
		struct toys children[4];
		int ctr;
		printf("Ваш выбор пал на Конструктор\n");
		for (ctr = 0; ctr < 3; ctr++)
		{
			printf("Введите цену игрушки %d:", (ctr + 1));
			scanf_s("%d", &children[ctr].price);
			puts("Введите возрастную категорию:");
			scanf_s("%d%d", &children[ctr].age1, &children[ctr].age2);
			system("cls");
		}
		printf("Информация о товаре Конструктор:\n");
		for (ctr = 0; ctr < 3; ctr++)
		{
			printf("Товар №%d:\n", (ctr + 1));
			printf("Цена:%d Возрасная категория:%d-%d\n", children[ctr].price, children[ctr].age1, children[ctr].age2);
		}
		break;
	}
	}
	return 0;
}

============================================================================================================================================================================================================================================================
#include <stdio.h>
#include <conio.h>
#include <windows.h>
#define SIZE 3
struct toys 
{
	int price;
	int age1;
	int age2;
};
int main()
{
	system("chcp 1251>0");
	int input;
	printf("Выбери игрушку:\n 1.Кукла\n 2.Кубик\n 3.Мяч\n 4.Конструктор\n");
	printf("Я выбираю:");
	scanf_s("%d", &input);
	system("cls");
	switch (input)
	{
	case 1:
	{
		struct toys children[4];
		int ctr;
		printf("Ваш выбор пал на куклу\n");
		for (ctr = 0; ctr < SIZE; ctr++)
		{
			printf("Введите цену игрушки %d:", (ctr + 1));
			scanf_s("%d", &children[ctr].price);
			puts("Введите возрастную категорию:");
			scanf_s("%d%d", &children[ctr].age1, &children[ctr].age2);
			system("cls");
		}
		printf("Информация о товаре Кукла:\n");
		for (ctr = 0; ctr < SIZE; ctr++)
		{
			printf("Товар №%d:\n", (ctr + 1));
			printf("Цена:%d Возрасная категория:%d-%d\n", children[ctr].price, children[ctr].age1, children[ctr].age2);
		}
		break;
	}
	case 2:
	{
		struct toys children[4];
		int ctr;
		printf("Ваш выбор пал на Кубик\n");
		for (ctr = 0; ctr < SIZE; ctr++)
		{
			printf("Введите цену игрушки %d:", (ctr + 1));
			scanf_s("%d", &children[ctr].price);
			puts("Введите возрастную категорию:");
			scanf_s("%d%d", &children[ctr].age1, &children[ctr].age2);
			system("cls");
		}
		printf("Информация о товаре Кубик:\n");
		for (ctr = 0; ctr < SIZE; ctr++)
		{
			printf("Товар №%d:\n", (ctr + 1));
			printf("Цена:%d Возрасная категория:%d-%d\n", children[ctr].price, children[ctr].age1, children[ctr].age2);
		}
		break;
	}
	case 3:
	{
		struct toys children[4];
		int ctr;
		printf("Ваш выбор пал на Мяч\n");
		for (ctr = 0; ctr < SIZE; ctr++)
		{
			printf("Введите цену игрушки %d:", (ctr + 1));
			scanf_s("%d", &children[ctr].price);
			puts("Введите возрастную категорию:");
			scanf_s("%d%d", &children[ctr].age1, &children[ctr].age2);
			system("cls");
		}
		printf("Информация о товаре Мяч:\n");
		for (ctr = 0; ctr < SIZE; ctr++)
		{
			printf("Товар №%d:\n", (ctr + 1));
			printf("Цена:%d Возрасная категория:%d-%d\n", children[ctr].price, children[ctr].age1, children[ctr].age2);
		}
		break;
	}
	case 4:
	{
		struct toys children[4];
		int ctr;
		printf("Ваш выбор пал на Конструктор\n");
		for (ctr = 0; ctr < SIZE; ctr++)
		{
			printf("Введите цену игрушки %d:", (ctr + 1));
			scanf_s("%d", &children[ctr].price);
			puts("Введите возрастную категорию:");
			scanf_s("%d%d", &children[ctr].age1, &children[ctr].age2);
			system("cls");
		}
		printf("Информация о товаре Конструктор:\n");
			for (ctr = 0; ctr < SIZE; ctr++)
			{
				printf("Товар №%d:\n", (ctr + 1));
				printf("Цена:%d Возрасная категория:%d-%d\n", children[ctr].price, children[ctr].age1, children[ctr].age2);
			}
			int i, j,tmp;
			for (i = 0; i < SIZE - 1; i++)
			{
				for (j = 0; j < SIZE - 1; j--)
				{
					if (children[ctr].price < children[ctr - 1].price)
					{
						tmp = children[ctr - 1].price;
						children[ctr - 1].price = children[ctr].price;
						children[ctr].price = tmp;
					}
				}
			}
		    printf("Цена самого дорогого конструктора:%d", tmp);
			break;
		}
	}
	return 0;
}
============================================================================================================================================================================================================================================================
#include <stdio.h>
#include <conio.h>
#include <windows.h>
#define SIZE 3
struct toys
{
	char toy1[50];
	int price;
	int age1;
	int age2;
};
int main()
{
	system("chcp 1251>0");
	struct toys children[4];
	int ctr=0;
	int i=0;
	printf("Введите название игрушки:");
	gets_s(children[ctr].toy1);
	rewind(stdin);
	printf("Введите цену для %s:", children[ctr].toy1);
	for (ctr = 0,i=0; ctr < SIZE,i<SIZE; ctr++,i++)
	{
		{
			scanf_s("%d", &children[ctr].price);
		}
		printf("Цена %d:%d\n",(i+1),children[ctr].price);
	}
	int a, b;
	int tmp = 0;
	for (a = 0; a < SIZE; a++)
	{
		for (b = 0; b < SIZE; b--)
		{
			if (children[ctr].price < children[ctr-1].price)
			{
				tmp = children[ctr - 1].price;
				children[ctr - 1].price = children[ctr].price;
				children[ctr].price = tmp;
			}
		}
	}
	printf("Максимальная цена игрушки %s - %d",children[ctr].toy1,tmp);
	return 0;
}
============================================================================================================================================================================================================================================
#include <stdio.h>
#include <conio.h>
#include <windows.h>
#define SIZE 3
struct toys
{
	char toy1[50];
	int price;
	int age1;
	int age2;
};
int main()
{
	system("chcp 1251>0");
	struct toys children[3];
	int ctr = 0;
	int i = 0;
	printf("Введите название игрушки:");
	gets_s(children[ctr].toy1);
	rewind(stdin);
	int pr; int tt;
	for (pr = 0; pr < strlen(children[ctr].toy1); pr++)
	{
		if (children[ctr].toy1[i] == 'k')
		{
			printf("Konstructor?\n1-da;2-net");
			scanf_s("%d", &tt);
			if (tt == 1)
			{
				for (ctr = 0; ctr < SIZE; ctr++)
				{
					{
						printf("Цена:");
						scanf_s("%d",&children[ctr].price);
						printf("Возраст:");
						scanf_s("%d %d",&children[ctr].age1, &children[ctr].age2);
					}
					system("cls");
				}
				int a, b;
				int tmp = 0;
				for (a = 0; a < SIZE; a++)
				{
					for (b = 0; b < SIZE; b--)
					{
						if (children[ctr].price < children[ctr - 1].price)
						{
							tmp = children[ctr - 1].price;
							children[ctr - 1].price = children[ctr].price;
							children[ctr].price = tmp;
						}
					}
				}
				for (ctr = 0; ctr<1; ctr++)
				{
					printf("Максимальная цена игрушки %s равняется %d$\n", children[ctr].toy1, tmp);
				}
			}
			else
			{
				printf("Введите цену для %s:", children[ctr].toy1);
				for (ctr = 0, i = 0; ctr < SIZE, i < SIZE; ctr++, i++)
				{
					{
						printf("Цена");
						scanf_s("%d", &children[ctr].price);
						printf("Возраст");
						scanf_s("%d %d", &children[ctr].age1, &children[ctr].age2);
					}
					system("cls");
				}
			}
		}
	}
	for (ctr = 0; ctr < SIZE; ctr++)
	{
		printf("Цена %d:%d$\n", (ctr + 1), children[ctr].price);
		printf("Возраст %d:%d-%d\n", (ctr + 1), children[ctr].age1, children[ctr].age2);
	}
	return 0;
}


===========================================================================================================================================================================================================================================

#include <stdio.h>
#include <conio.h>
#include <windows.h>
#define a 20

struct toys
{
	char toy1[a];
	int price;
	int age1;
	int age2;
};

int main()
{
	system("chcp 1251>0");
	toys *s;
	char k[a] = "konstructor";
	int n;
	int f = 0, max = 0;
	printf("Введите количество игрушек:");
	scanf_s("%d", &n);
	if (!(s = (toys*)malloc(n * sizeof(toys))))
		return 1;
	for (int i = 0; i < n; i++)
	{
		printf("Введите название %d игрушки:", i + 1);
		rewind(stdin);
		gets_s(s[i].toy1);
		do
		{
			rewind(stdin);
			printf("Введите цену игрушки: ");
		} while (!scanf_s("%d", &s[i].price));
		printf("Введите возрастные ограничения:\n");
		do
		{
			rewind(stdin);
			printf("От: ");
		} while (!scanf_s("%d", &s[i].age1));
		do
		{
			rewind(stdin);		
			printf("До:  ");
		} while (!scanf_s("%d", &s[i].age2));
	}
	for (int i = 0; i < n; i++)
	{
		for (int j = 0; j < strlen(k); j++)
		{
			if (k[j] == s[i].toy1[j])
				f++;
		}
		if (f == strlen(k))
		{
			if (s[i].price > max)
				max = s[i].price;
		}
		f = 0;
	}
	if (max == 0)
		printf("Игрушки конструктор нет\n");
	else
		printf("Цена самого дорогого конструктора: %d\n", max);
	return 0;
}