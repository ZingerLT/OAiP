//11.Ввести k-целое число и строку, состоящую из N (N≤k) символов.
//Исходную строку разбить на подстроки длиной не более 30 символов и вывести. 
//Между словами оставить по одному пробелу.
//Переход на новую строку осуществлять на месте пробела.
#include <stdio.h>
#include <stdlib.h>
#include <Windows.h> // подключение библиотек
#include <string.h>
int main()
{
	system("chcp 1251>0"); // включение русского языка в консоль
	char *text = 0;
	int k, i, a;
	system("cls"); // очистка консоли
	printf("Введите значение k:"); //вывод строки в консоль
	scanf_s("%d", &k);//передача данных в переменную К
	rewind(stdin); //вызов функции rewind позволяет переключаться между режимами чтения и записи
	text = (char*)malloc(k * sizeof(char));//динамическое выделение памяти под переменную
	printf("Введите строку:");//вывод строки в консоль
	gets_s(text, k);//считывание строки(с пробелами)
	int N = 0;
	a = strlen(text);
	for (i = 0; i < a; i++)//начало цикла for
	{

		if (N >= 30) // первое условие
		{
			printf("\n");
			N = 0;
		}
		if (text[i] == ' ')// второе условие
		{
			printf("\n");
			N = 0;
		}
		printf("%c", text[i]);// вывод окончательной символов в консоль
		N++;
	}
	free(text);// освобождение памяти
	getchar();
	return 0;
}
